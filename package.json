{
  "name": "anyprint-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm run -r build",
    "test": "pnpm run -r test",
    "package": ""
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}

